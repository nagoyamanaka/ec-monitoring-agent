import { Router } from "express";
import { QueryBus } from "../../../../../Contexts/Shared/domain/QueryBus.js";

// TODO(task7): GET /analytics コントローラを実装する
export function registerAnalyticsRoutes(
  _router: Router,
  _queryBus: QueryBus,
): void {}
