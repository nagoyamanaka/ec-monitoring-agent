{
  "name": "video-capture",
  "private": true,
  "type": "module",
  "description": "提出動画（docs/protopedia/video/script.md）の素材テイク（編集用 H.264 mp4・ロスレス webm 源は .raw/）とスクショを Playwright で自動採取する撮影ツール。本体 pnpm workspace には属さない（pnpm install --ignore-workspace で独立インストール）。",
  "scripts": {
    "setup": "pnpm install --ignore-workspace && playwright install chromium",
    "capture": "node capture.mjs"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "playwright": "^1.45.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "ffmpeg-static"
    ]
  }
}
